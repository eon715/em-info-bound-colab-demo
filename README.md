# em-info-bound-colab-demo
simple demo simulation that: creates fake-but-structured EM-like waves computes a simple “information” number checks a bound-like ratio produces plots you can show to licensees
